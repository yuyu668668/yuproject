import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  state: 0
}

const mutations = {}

const actions = {}

export default new Vuex.Store({
  state,
  mutations,
  actions
})
